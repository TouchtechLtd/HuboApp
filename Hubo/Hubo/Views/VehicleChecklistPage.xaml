<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:Hubo;assembly=Hubo"
             x:Class="Hubo.VehicleChecklistPage">
  <Grid>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="*" />
      <ColumnDefinition Width="3*" />
      <ColumnDefinition Width="5*" />
      <ColumnDefinition Width="*" />
    </Grid.ColumnDefinitions>
    <Grid.RowDefinitions>
      <RowDefinition Height="6*" />
      <RowDefinition Height="*" />
      <RowDefinition Height="*" />
      <RowDefinition Height="3*" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>
    
    <Label Text="{Binding HuboText}" Grid.Column="1" Grid.Row="1"/>
    <local:BaseEntry x:Name="huboEntry" Text="{Binding HuboEntry}" Grid.Column="2" Grid.Row="1" Keyboard="Numeric"/>
    
    <Label Text="{Binding LocationText}" Grid.Column="1" Grid.Row="2"/>
    <local:BaseEntry x:Name="locationEntry" Text="{Binding LocationEntry}" Grid.Column="2" Grid.Row="2"/>
    
    <Button Text="{Binding ContinueText}" Grid.Column="1" Grid.ColumnSpan="2" Grid.Row="3" Command="{Binding ContinueCommand}" />
  </Grid>
</ContentPage>