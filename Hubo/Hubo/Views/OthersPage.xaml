<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Hubo.OthersPage"
             Style="{StaticResource ContentPageStyle}"
             >

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <RelativeLayout Grid.Row="0" Grid.Column="0">
            <Image Source="DesertRoadjpg.png" Aspect="AspectFill" />
            <StackLayout RelativeLayout.XConstraint="{ConstraintExpression Type=Constant, Constant=8}" RelativeLayout.YConstraint="{ConstraintExpression Type=Constant, Constant=140}" RelativeLayout.WidthConstraint="{ConstraintExpression Type=RelativeToParent, Property=Width, Constant=-16}" RelativeLayout.HeightConstraint="{ConstraintExpression Type=Constant, Constant=48}" Orientation="Horizontal">
                <Label Text="{Binding Name}"  TextColor="White" FontSize="22" YAlign="End" VerticalOptions="End" HorizontalOptions="FillAndExpand" VerticalTextAlignment="End" />
            </StackLayout>
        </RelativeLayout>

        <ListView x:Name="othersList" ItemsSource="{Binding OthersPageList}" HasUnevenRows="True" SeparatorVisibility="None" Grid.Column="0" Grid.Row="1">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell >
                        <ViewCell.View >
                            <StackLayout Orientation="Horizontal" HorizontalOptions="FillAndExpand" VerticalOptions="FillAndExpand" Padding="15,12,0,12"  >
                                <Image Source="{Binding ImageSource}" HeightRequest="30"/>
                                <Label Text="{Binding Title}" YAlign="Center" FontSize="14" HorizontalTextAlignment="Center" XAlign="Center" HorizontalOptions="Center"  TextColor="Black"/>
                            </StackLayout>
                        </ViewCell.View>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</ContentPage>