<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Hubo.LandingPage">
  <Grid RowSpacing="0" ColumnSpacing="0">
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="*" />
      <ColumnDefinition Width="*" />
    </Grid.ColumnDefinitions>
    <Grid.RowDefinitions>
      <RowDefinition Height="8*" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>
    <Button Text="{Binding Login}" Command="{Binding LoginButton}" Grid.Column="0" Grid.Row="1" AutomationId="LoginButton"/>
    <Button Text="{Binding Register}" Command="{Binding RegisterButton}"  Grid.Column="1" Grid.Row="1" AutomationId="RegisterButton"/>
  </Grid>
</ContentPage>